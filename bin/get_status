#!/bin/sh

ambient_temperature () {
    echo -n " "
    cat /tmp/temper.deg || echo "N/A"
}

mail () {
    mail_notify
}

date_time () {
    date '+%Y-%m-%d %H:%M'
}

echo " $(ambient_temperature) | $(mail) | $(date_time)"
